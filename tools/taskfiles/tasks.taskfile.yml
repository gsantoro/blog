# https://taskfile.dev

version: '3'

includes:
  github: ./github.taskfile.yml

tasks:
  default:
    cmds:
      - task: download
        vars:
          TASK_NAME: devenv

      - task: download
        vars:
          TASK_NAME: hugo

      - task: download
        vars:
          TASK_NAME: magick
      
      - task: download
        vars:
          TASK_NAME: img

  download:
    cmds:
      - task: github:download
        vars: 
          GITHUB_REPO: automation
          GITHUB_PATH: tools/{{.TASK_NAME}}.taskfile.yml
          LOCAL_PATH: tools/taskfiles/lib/{{.TASK_NAME}}.taskfile.yml
