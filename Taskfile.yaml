# https://taskfile.dev

version: '3'

includes:
  env: tools/taskfiles/env.taskfile.yaml
  hugo: tools/taskfiles/hugo.taskfile.yaml
  devenv: tools/taskfiles/devenv.taskfile.yaml

tasks:
  default:  
    - task: hugo